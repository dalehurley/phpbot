# Copy this file to .env and fill in the required values
ANTHROPIC_API_KEY=YOUR-ANTHROPIC-API-KEY-HERE
PHPBOT_TIMEOUT=120
PHPBOT_WS_PORT=8788
PHPBOT_WS_UDP_PORT=8789

# Models
PHPBOT_FAST_MODEL=claude-haiku-4-5
PHPBOT_MODEL=claude-sonnet-4-5
PHPBOT_SUPER_MODEL=claude-opus-4-5

# Limits
PHPBOT_MAX_ITERATIONS=20
PHPBOT_MAX_TOKENS=4096
PHPBOT_TEMPERATURE=0.7

# Paths
PHPBOT_TOOLS_STORAGE_PATH=/absolute/path/to/storage/tools
PHPBOT_SKILLS_PATH=/absolute/path/to/skills
PHPBOT_KEYS_STORAGE_PATH=/absolute/path/to/storage/keys.json
PHPBOT_WORKING_DIRECTORY=/absolute/path/to/working/dir

# Router Classifier
# Controls which LLM classifies requests that DON'T match the local cache.
# Most requests are handled by the built-in PHP native TF-IDF classifier
# (zero tokens, instant). Only truly ambiguous requests need an LLM.
#
# Provider: auto, apple_fm, mlx, ollama, lmstudio, groq, gemini, anthropic
# 'auto' priority: Apple FM → MLX → Ollama → LM Studio → Groq → Gemini → Anthropic
PHPBOT_CLASSIFIER_PROVIDER=auto

# Apple Foundation Models (macOS 26+, on-device, free, no setup needed)
# Uses the on-device Apple Intelligence model via a Swift bridge.
# The bridge auto-compiles on first use if swiftc is available.
# No configuration needed — just works on macOS 26 (Tahoe) and later.

# MLX (Apple Silicon, tiny model, free, works offline)
# A Python HTTP server running a tiny model on Metal/GPU.
# Setup:
#   pip install mlx-lm
#   python bin/mlx-classify-server.py
# Optional: specify a different model with --model flag
PHPBOT_CLASSIFIER_MLX_URL=http://localhost:5127

# Ollama (local, free, works offline)
# Install: brew install ollama && ollama pull qwen2.5:1.5b
PHPBOT_CLASSIFIER_OLLAMA_URL=http://localhost:11434
PHPBOT_CLASSIFIER_OLLAMA_MODEL=qwen2.5:1.5b

# LM Studio (local, free, GUI-based)
# Download from https://lmstudio.ai, load any small model, enable local server
PHPBOT_CLASSIFIER_LMSTUDIO_URL=http://localhost:1234

# Groq (free tier, recommended cloud option)
# Sign up at https://console.groq.com to get a free API key
GROQ_API_KEY=
PHPBOT_CLASSIFIER_GROQ_MODEL=llama-3.3-70b-versatile

# Google Gemini (very cheap cloud option)
# Get API key at https://aistudio.google.com
GEMINI_API_KEY=
PHPBOT_CLASSIFIER_GEMINI_MODEL=gemini-3-flash-preview

# Commands
# Use a comma-separated list or a JSON array for complex entries.
PHPBOT_BLOCKED_COMMANDS=["rm -rf /","rm -rf /*","mkfs","dd if=","(){:|:&};:","> /dev/sda","chmod -R 777 /"]
PHPBOT_ALLOWED_COMMANDS=
